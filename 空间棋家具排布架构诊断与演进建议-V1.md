# 空间棋家具排布架构诊断与演进建议（V1）

> 报告版本：V1.0  
> 报告日期：2026-08-10  
> 项目目标：以“下棋化”的方式生成快速、优质、可解释、可局部控制的房间家具排布方案。

---

## 一、项目初心

项目希望把房间内的家具排布抽象为一盘“空间棋”：

- 房间是棋盘；
- 家具是棋子；
- 家具的位置、朝向、尺寸模数和相互关系是走法；
- 一步步落子形成完整布局；
- 搜索过程能够像棋类 AI 一样快速找到较优方案；
- 最终布局既满足碰撞、通行和使用要求，也具备较好的空间构图和设计质量。

家具并不是单一固定尺寸。同一类棋子可以拥有不同模数，例如单人床、双人床，不同长度的衣柜、书桌，以及单人、双人、三人沙发。系统需要在房间轮廓、门窗、面积、模数和家具组合均不固定的情况下，仍然快速形成合理布局。

## 二、当前结论

当前技术方向没有完全偏离“空间棋”，但实现已经逐渐从“棋谱驱动的空间搜索”偏向：

> 单件家具枚举 + 大量几何采样 + 规则补丁 + Beam Search 保留候选。

系统仍具有棋盘、棋子、逐步落子和搜索树等形式，但真正决定棋类系统效率的“局型识别、少量高价值走法、模块棋、棋谱复用和稳定局势评价”尚未成为搜索的主干。

当前增加采样点和 Beam 宽度，主要是在用更多计算量弥补动作表达能力不足。其直接结果是：

- 局部关系越来越难独立控制；
- 修改一个小规则可能改变整盘搜索结果；
- 候选节点数量快速膨胀；
- 搜索耗时容易超过 1 秒；
- 评分、剪枝与后处理逻辑越来越复杂；
- 系统虽然能够生成结果，但越来越难解释“为什么是这一步”。

## 三、当前系统的实际搜索方式

当前系统的主要流程如下：

1. 根据面积和目标尝试若干套家具数量组合；
2. 将床、床头柜 A、床头柜 B、衣柜、书桌、椅子等展开为独立家具槽位；
3. 按配置顺序逐件落子；
4. 为当前家具生成墙面点、相对关系点、房间分区点和尺寸变体；
5. 对每个 Beam 局面扩展候选动作；
6. 执行越界、碰撞、功能区、门区、通行和孤岛检查；
7. 使用局部分、前向检查和阶段性地面评价保留一批局面；
8. 搜索结束后进行完整质量评分、方案去重和后处理补齐。

当前代码中的主要对应位置：

- `assets/js/space-chess.js` 中的 `buildFurniture()`：把家具数量展开成独立棋子；
- `rawCandidatesForItem()`：对家具模数分别生成位置候选；
- `searchMatrix()`：按照家具顺序逐轮扩展 Beam；
- `autoSelectInventory()`：切换多套家具库存并重复启动搜索；
- `evaluateFull()`：在终局统一计算通行、关系、模块、地面、墙面和构图质量。

当前配置中，默认 Beam 宽度为 120；部分小房间丰富模式最低 Beam 宽度可达到 240；单个父局面候选上限可达到 72；普通房间家具库存方案最多尝试 12 次。

因此，整体搜索量近似为：

```text
库存方案数量
× 家具槽位数量
× Beam 宽度
× 单件家具候选数
× 前向检查、通行检查和评分成本
```

Bitset 和矩阵碰撞已经降低了单次判断成本，但没有解决节点生成总量过大的问题。

## 四、为什么“像象棋”不等于“逐件家具搜索”

象棋 AI 的高速度建立在高度离散和长期结构化的基础上：

- 棋盘位置固定；
- 每类棋子的合法动作有限；
- 局面可以稳定哈希和复用；
- 有成熟的局势评价；
- 有开局库、残局库、置换表和强力剪枝；
- 现代棋类 AI 还能利用策略模型优先推荐少量高价值动作。

房间排布则具有以下不同点：

- 坐标接近连续空间；
- 家具存在尺寸、数量、朝向和组合选择；
- 房间轮廓和门窗条件变化很大；
- 没有对手，不属于典型 Minimax 问题；
- 单件家具是否是好棋，往往需要等待相关家具全部完成后才能评价。

因此，空间排布更接近：

> 约束规划 + 几何装箱 + 设计语法 + 启发式搜索。

Beam Search 可以继续使用，但“使用 Beam Search”本身不代表已经完成真正的棋化。

## 五、棋子的正确抽象层级

家具应当保持为独立物理对象，但搜索动作不应始终等同于单件家具。

### 5.1 两个不同层面的棋子

**物理对象层：**

- 每张床、每个床头柜、每把椅子保持独立；
- 用于精确碰撞、渲染、编辑、报价和最终输出。

**搜索动作层：**

- 强关联家具可以组成一次“模块棋”；
- 模块内部一次确定主要关系，再展开为独立实体进行验收。

### 5.2 建议的划分

| 家具关系 | 推荐搜索方式 | 原因 |
|---|---|---|
| 床 + 床头柜 | 参数化模块棋 | 床头柜位置几乎完全由床决定 |
| 餐桌 + 餐椅 | 参数化模块棋 | 需要整体判断餐组完整性 |
| 沙发 + 电视 + 茶几 | 先形成会客骨架，再补内部对象 | 三者强关联，但不同房型存在较多变例 |
| 衣柜、书桌、边柜 | 单件棋或附着棋 | 通常依附墙段、窗边或已有功能组 |
| 绿植、落地灯、填缝柜 | 终局附着或后处理 | 不值得扩张主搜索树 |

### 5.3 参数化床组示例

```text
BedGroupMove {
  anchorWall
  bedVariant: 1.2 / 1.5 / 1.8
  alignment: left / center / right
  nightstandMode: none / left / right / symmetric
  nightstandVariant: 0.35 / 0.45 / 0.55
  footBench: none / compact / standard
}
```

床组仍会展开为床、床头柜和床尾凳等独立实体，但全局搜索看到的是一手结构完整的模块棋，而不是先下床，再等待若干回合后判断床头柜是否还放得下。

## 六、是否必须先知道“什么是好布局”

系统需要先建立一组“足够好的布局定义”，但不需要寻找唯一的世界最优解。

室内布局是多目标问题，常见目标包括：

- 通行连续；
- 家具使用方便；
- 功能组完整；
- 收纳充足；
- 空间丰满但不拥挤；
- 构图平衡；
- 采光合理；
- 局部关系符合使用习惯。

这些目标之间可能冲突。系统首先需要的是：

1. 不可违反的硬约束；
2. 可分别观察的质量维度；
3. 一批经过人工认可的参考布局；
4. 不同房间类型和拓扑条件下允许的布局原型；
5. 同一种布局类型内部的排序规则。

推荐的评价顺序为：

```text
硬约束验收 → 布局类型匹配 → 类型内部多目标排序
```

严重问题不应通过其他高分抵消。例如通路断裂、柜门无法使用、必选功能组残缺，应直接淘汰，而不是仅扣除一部分总分。

## 七、如何规整和保存好的布局

好的布局不能主要保存为绝对坐标。以下表达缺乏复用能力：

```text
床位于 x=2.15m，y=0.90m。
```

更适合保存为“关系图 + 参数范围 + 房间拓扑条件”：

```text
床组：
- 床头贴完整实墙；
- 床两侧至少一侧保留床头柜；
- 双床头柜优先对称；
- 床尾朝向主要活动面；
- 衣柜不得侵入床侧起身区；
- 工作区优先匹配采光墙；
- 入口到床侧、衣柜和窗边形成连续通路。
```

### 7.1 棋谱示例

```text
适用条件：
- 卧室；
- 近矩形；
- 面积 12–18㎡；
- 至少存在一段 2.8m 完整实墙；
- 门不位于该墙中央。

开局：
1. 床组落在最长完整实墙；
2. 收纳组落在与床墙相邻的次长墙；
3. 工作组匹配采光墙；
4. 剩余连续空区作为活动区。

变例：
- 床墙不足：切换单床头柜模式；
- 窗占据工作墙：书桌转移到相邻墙；
- 房间狭长：切换酒店式床尾媒体墙。
```

### 7.2 房间不应只按面积分类

面积只是一个特征。更有价值的房间拓扑特征包括：

- 完整墙段长度序列；
- 门窗所在墙及切割位置；
- 房间主轴方向；
- 长宽比；
- 凹槽数量与尺寸；
- 入口到各功能区域的连通关系；
- 可用墙面的相邻关系；
- 中央连续空区的形状和尺寸。

两个面积不同但拓扑相似的房间可以复用同一种棋谱；两个面积相同但门窗结构完全不同的房间不应被强行归为同一类。

## 八、建议的分层空间棋架构

```text
房间拓扑识别
    ↓
匹配少量布局棋谱
    ↓
搜索功能模块骨架
    ↓
模块内部选择家具模数和关系
    ↓
少量连续参数微调
    ↓
硬约束验收与多目标排序
```

### 8.1 第一层：布局骨架

只决定：

- 哪段墙分配给哪个功能组；
- 启用哪些功能模块；
- 模块之间的拓扑关系；
- 主通路和主要活动区的位置。

这一层应只有约 5–20 个高价值动作。

### 8.2 第二层：模块棋

主要模块包括：

- 床组；
- 收纳组；
- 工作组；
- 会客组；
- 餐组。

如果候选动作具有明确语义，Beam 宽度可能只需要 8–24，而不是当前的 120–240。

### 8.3 第三层：模数选择

骨架确定后再选择：

- 床宽；
- 书桌长度；
- 沙发人数模数；
- 餐桌尺寸；
- 餐椅数量；
- 柜体组合长度。

这样可避免家具尺寸与全房间所有坐标形成巨大的组合乘积。

### 8.4 第四层：局部微调

不再主要依赖均匀采样，而是生成少量几何临界位置：

- 墙段起点和终点；
- 墙段中心；
- 与相邻家具边缘对齐的位置；
- 与门窗净距相切的位置；
- 与功能区边缘相切的位置；
- 柜体模数恰好收口的位置。

优秀布局更可能出现在这些具有设计语义的临界位置，而不是任意的均匀网格点。

## 九、当前系统值得保留的部分

现有项目不需要推倒重来。以下基础能力具有较高价值：

- 房间多边形和旋转矩形几何；
- 精确碰撞检测；
- Bitset broad phase；
- 门窗和功能区几何；
- 通行水漫与零孤岛检测；
- 家具尺寸模数配置；
- 最终布局质量诊断；
- 搜索树可视化；
- 已积累的布局样例、截图和回归测试。

建议重点重构搜索上层，而不是重新开发几何内核：

```text
当前：
家具清单 → 单件逐步枚举 → 大 Beam → 终局寻找完整关系

建议：
房间拓扑 → 棋谱骨架 → 模块动作 → 少量微调 → 终局验收
```

## 十、当前局部控制困难的根因

当前一个局部需求，例如“双床头柜必须对称”，可能同时影响：

- 床的墙面候选；
- 床头柜关系候选；
- 家具落子顺序；
- 单步奖励；
- 对称性终局评分；
- Beam 分支保留；
- 可选家具跳过成本；
- 前向检查；
- 最终丰富度门槛。

因此，修改一个局部关系会波及整盘搜索。

如果床和床头柜被定义为床组模块，这个局部要求只存在于床组内部。全局搜索只需要关心床组的占地、通行影响、完整度和总体代价，局部控制将明显简化。

长期还应建立模块依赖图。当用户锁定或调整某个家具时，只重新生成受影响的模块和邻接模块，而不是重新搜索整个房间。

## 十一、第一阶段落地建议

建议暂时停止继续增加采样点和 Beam 宽度，先完成一个范围受控的架构实验。

### 实验范围

1. 只处理矩形卧室；
2. 只保留床组、衣柜和工作组三类模块；
3. 准备约 20 个经过人工认可的布局；
4. 从参考布局中提炼 3–5 种卧室棋谱；
5. 将床和床头柜改为一次模块落子；
6. 墙面候选只使用端点、中心、对齐和相切位置；
7. Beam 宽度限制为 16 或 24；
8. 单次生成目标控制在 100–300ms；
9. 与当前系统比较质量、节点数、耗时和局部可控性。

### 第一阶段验收指标

- 参考房型硬约束通过率 100%；
- 床组、工作组和收纳组完整性可单独解释；
- 每种输出方案能够说明使用了哪种棋谱和哪些变例；
- 单件家具的局部修改不会无理由改变无关模块；
- 搜索节点数相对当前系统下降一个数量级；
- 常规矩形卧室单次生成不超过 300ms；
- 增加家具模数时，搜索量不随“模数 × 全部位置”线性膨胀。

## 十二、最终判断

项目真正需要的，不是让每件家具拥有越来越多的采样走法，而是让系统具备类似棋手的能力：

1. 先识别房间局型；
2. 匹配少量可复用棋谱；
3. 优先下完整的功能模块棋；
4. 在模块内部选择家具模数；
5. 只对少量关键参数进行局部微调；
6. 使用硬约束和多目标评价完成终局验收。

现有系统最值得保留的是几何内核、通行验收和诊断体系；最需要调整的是搜索动作的抽象层级。

> 空间棋的核心，不是“把家具一个个放进去”，而是“用少量有意义的模块动作，快速形成完整、可用、可解释的空间关系”。
