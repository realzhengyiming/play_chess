# 空间棋 AI Prototype 09：最新流程文档

> 对应程序：`bedroom-space-chess.html` v13  
> 目标：说明一次新户型如何从输入变成三套家具方案，以及每一步为什么快。

## 1. 一句话流程

```text
输入户型
→ 编译棋盘
→ 自动选家具清单
→ 按家具顺序生成语义候选点
→ Bitmask 批量粗筛
→ 精确几何与家具规则复核
→ Beam 多步搜索
→ Bitset 水漫检查核心可达
→ 完整终局评分
→ 选择三套结构不同的方案
```

## 2. 输入阶段

当前输入：

```text
空间类型：卧室 / 客厅
轮廓：矩形 / L 形 / 切角 / 凹槽
基础宽深
面积倍率 0.5×–3×
自动或手动家具模式
家具尺寸与数量（手动模式）
```

面积倍率只改变房间，家具保持真实米制尺寸。

未来接入真实户型时，建议统一为：

```json
{
  "polygon_mm": [],
  "doors": [],
  "windows": [],
  "columns": [],
  "fixed_objects": [],
  "room_type": "bedroom"
}
```

## 3. 第一步：编译新棋盘

输入户型到来后，只重新计算场景相关数据：

1. 多边形面积与质心；
2. 每条墙的长度、切向、内法线；
3. 门洞、门扇禁区和入口种子；
4. 窗口所在墙段和占用区间；
5. 归一化功能区投影；
6. 墙面关键锚点；
7. 0.12 m 碰撞 Bitset 上下文；
8. 默认 0.14 m 流线网格上的三种人体半径房间 Mask。

家具规则、评价器和搜索器不随户型重写。

结果：

```text
新户型 → 新 SceneCache
旧规则 → 继续复用
```

## 4. 第二步：自动选择家具清单

系统不是先把家具数量固定死，而是产生清单前沿。

### 4.1 最低清单

```text
卧室：床 + 衣柜
客厅：沙发 + 电视柜 + 茶几
```

### 4.2 逐步扩充

可能动作包括：

- 增加床头组；
- 增加书桌和工作椅；
- 增加餐桌和餐椅；
- 增加餐边柜、书柜、展示柜；
- 增加单椅、边几、灯和绿植；
- 切换家具紧凑/标准/宽裕尺寸档；
- 切换沙发类型。

### 4.3 便宜预估

先检查：

```text
房间面积是否支持
有效家具密度是否过高
沿墙家具总需求是否超过可用墙长
功能覆盖是否足够
家具件数是否超过软容量
```

每个目标保留少量代表清单，再进入真实布局搜索。

## 5. 第三步：确定落子顺序

家具按依赖关系排列：

```text
核心家具在前
依赖核心家具的关系家具在后
小件和填充家具最后
```

卧室示例：

```text
床 → 床头柜/床尾凳 → 衣柜 → 书桌 → 工作椅 → 其他收纳和小件
```

客厅示例：

```text
沙发 → 电视 → 茶几 → 单椅/边几/脚凳 → 餐桌 → 餐椅 → 沿墙柜和软装
```

这不是说第一步必须局部最优，而是先生成后续家具赖以产生候选点的核心坐标系。

## 6. 第四步：生成当前家具候选点

每个父局面单独生成当前家具动作。

### 6.1 沿墙点

从可用墙段的端点、中心、分数点和补点生成；连续柜还会根据剩余墙段生成不同宽度。

### 6.2 家具关系点

由已放家具的真实坐标直接计算：

```text
父家具中心
+ 横向方向 × 横向偏移
+ 正面方向 × 前后偏移
= 子家具中心
```

### 6.3 功能区点

把 0–1 归一化设计区投影到当前房间。

### 6.4 角落点

从角点向室内偏移家具半径和维护距离。

生成后先用位姿键去重。

## 7. 第五步：编译候选 Profile

每个候选第一次出现时生成并缓存：

```text
真实家具矩形
真实硬功能区矩形
静态合法性
家具本体 Body Mask
带通用净距的 Collision Mask
柜门/椅后退等 Action Mask
```

同一候选在不同搜索分支再次出现时，直接复用缓存。

## 8. 第六步：Bitmask 批量粗筛

当前 Beam 中所有父局面和候选被展平为一个批次。

### 8.1 家具实体

```text
父局面 _occ AND 候选 collisionMask
```

- 结果为 0：肯定不相交，直接通过；
- 结果非 0：只是疑似相交，进入精确复核。

### 8.2 功能区

```text
父局面 _hardOcc AND 候选 bodyMask
父局面 _occ     AND 候选 actionMask
```

检测新家具是否占用已有柜门区，以及新柜门区是否压住已有家具。

Bitmask 只是加速索引，不是摆放坐标。

## 9. 第七步：精确几何与规则复核

只对 Bitmask 报警的候选执行：

```text
真实矩形碰撞
家具专属碰撞净距
硬功能区冲突
配套家具例外
靠墙、避窗、门扇禁区等规则
```

### 9.1 床—床头柜零接缝示例

床头柜不是对齐到12cm格子，而是直接计算：

```text
床头柜中心距床中心
= 床宽/2 + 床头柜宽/2 + 0 mm
```

位图可能报警，因为零缝家具可能覆盖同一个粗格；精确检查确认两者只是边缘接触，然后由 `bed-side` 关系规则放行。

同时只允许床进入床头柜功能区的关系余量，其他家具仍会被拒绝。

因此：

```text
真实坐标决定摆放
Bitmask 决定是否需要进一步检查
```

## 10. 第八步：候选便宜排序与 Top-K

合法候选计算局部启发分，例如：

- 核心家具离入口远近；
- 书桌靠窗；
- 衣柜和电视避窗；
- 床头柜是否来自床侧；
- 电视是否面对沙发；
- 茶几是否位于沙发前；
- 参数化柜体是否合理填充剩余墙段。

每个父局面最多保留 72 个候选。

这里的分数只负责搜索排序，不代表最终设计总分，也不会把搜索退化成每步只选一个局部最优。

## 11. 第九步：生成下一层搜索树

每个合法动作形成子局面：

```text
新 poses
新 partialScore
新 _occ
新 _hardOcc
新真实矩形与功能区
新局面 Hash
```

重复局面只保留累计分更高者。

在分配完整 Bitset 前，弱分支先裁切到大约 `2 × Beam Width`；默认最终 Beam Width 为 120。

## 12. 第十步：提前防止未来无棋可走

如果还有下一件家具，系统批量生成下一件家具最多 22 个候选：

```text
下一件至少有一个合法点 → 当前分支继续
下一件一个合法点都没有 → 当前分支立即剪掉
```

这解决“前面步骤把后面的空间写死”的一部分问题。

它只检查下一手，不等于完全解决长程最优；Beam 中保留多个不同局面负责延缓过早承诺。

## 13. 第十一步：关键节点流线剪枝

当茶几、餐桌、衣柜、书桌等关键障碍落下后：

1. 将家具按最窄人体半径膨胀；
2. 从门口种子做 Bitset 水漫；
3. 检查核心家具使用入口是否属于同一连通域；
4. 如果核心入口已经断开，立即剪枝。

家具只会继续增加障碍，已经断开的连通域不会在后续自行恢复，所以这类剪枝具有单调性。

## 14. 第十二步：Beam 保留

所有子局面按部分分排序，保留前 120 个进入下一家具。

```text
不是只选当前第一名
而是同时保留最多120条不同棋谱前缀
```

所以一次局部不占优的动作，只要仍处于 Beam 内，就可能依靠后续更好的终局反超。

## 15. 第十三步：终局完整评价

家具全部落下后，才对少量终局执行完整评价：

```text
硬规则可行
三尺度核心可达
功能覆盖
家具关系
构图组织
沿墙收纳连续性
工学舒适
采光
```

总分权重：

```text
功能15 + 通行20 + 关系20 + 构图20
+ 收纳10 + 舒适10 + 采光5
```

同时使用质量门槛，防止某一高分项掩盖严重短板。

## 16. 第十四步：选出三套不同方案

从合格终局池依次选择：

```text
A 综合总分最高
B 通行和舒适优先
C 构图、关系和收纳优先
```

新方案必须与已选方案达到最低位移/旋转差，否则继续寻找下一个终局。

## 17. 第十五步：生成可回看的推演轨迹

界面以终局中的家具顺序重建演示棋谱：

```text
步骤0：空房间，显示第一手候选
步骤1：放入第一件，显示第二手候选
步骤2：放入第二件，显示第三手候选
……
终局：全部家具完成
```

候选点图例：

```text
颜色：筛选结果
形状：生成来源
```

拖动进度条只重绘棋盘，不刷新页面。

## 18. 为什么整体速度快

速度来自多层叠加：

1. 只生成设计上有意义的候选点；
2. 静态规则在候选 Profile 中缓存；
3. Bitset 一次比较32个格子；
4. 没有位图相交的候选不做精确检查；
5. 关系接触只增加极少数矩形复核；
6. 每个父局面限制72个动作；
7. Beam 限制为120个局面；
8. 下一手无点的分支提前删除；
9. 核心流线断开时提前删除；
10. 完整评分只作用于少量终局。

## 19. 新户型进入后哪些重算、哪些复用

| 内容 | 新户型是否重算 |
| --- | --- |
| 房间多边形、墙、门窗 | 重算 |
| 归一化设计区投影 | 重算 |
| 静态墙候选 | 重算 |
| 关系候选 | 随搜索动态生成 |
| 候选 Bitmask Profile | 新位姿首次出现时生成并缓存 |
| 家具类型规则 | 复用 |
| 关系规则 | 复用 |
| 评价器 | 复用 |
| 搜索器 | 复用 |

因此新户型的成本是“重新编译一张棋盘”，不是重新建立无限棋谱，也不是重新训练模型。

## 20. NumPy 复刻流程

推荐模块划分：

```text
scene.py       户型和墙面编译
inventory.py   家具清单前沿
candidates.py  四类走法生成器
profiles.py    实体/功能区和 Bitmask 缓存
legality.py    NumPy/Numba 批量合法性
search.py      Beam、去重、前向检查
flow.py        uint64 水漫
evaluate.py    终局评价
select.py      三方案差异化选择
```

最核心的矩阵判断：

```python
parent_occ = occupancy[parent_index]

body_hit = np.any(
    (parent_occ & candidate_collision_masks) != 0,
    axis=1,
)

legal = static_legal & ~body_hit
```

疑似相交索引再交给 Numba 或向量化真实矩形复核。真实坐标建议使用整数毫米，Bitmask 仍保持 8–12 cm 粗筛。

## 21. 当前流程中最重要的原则

```text
硬规则负责绝不犯低级错误
语义候选负责减少无意义搜索
Bitmask负责批量加速
精确几何负责真实尺寸
Beam负责保留多条未来
终局评分负责比较完整方案
设计师数据负责未来校准规则与权重
```
