对，你理解基本正确。

**家具偏好配置（Furniture Preference / Rule Config）主要影响三个阶段：**

```
1. 候选 Pose 生成
        ↓
2. 候选过滤（淘汰不合理位置）
        ↓
3. 落子评分 S(x)（给合理位置排序）
```

但是三个阶段作用不同。

------

# 1. 影响候选 Pose 生成

作用：

> 决定“哪些位置值得尝试”。

例如：

## 衣柜配置

```
{
 type:"wardrobe",
 preferWall:true,
 avoidWindow:true
}
```

生成候选：

可能只生成：

```
墙1：
左、中、右

墙2：
左、中、右
```

不会生成：

```
房间中央
```

因为衣柜偏好靠墙。

------

## 书桌配置

```
{
 preferWindow:true
}
```

候选优先生成：

```
窗边区域
```

而不是整个房间随机位置。

------

## 茶几配置

```
{
 relation:"sofa-front"
}
```

候选直接来自：

```
沙发前方
```

不会生成：

```
房间角落茶几
```

------

# 2. 影响过滤（你问的重点）

这里不是“评分扣分”。

而是：

> 这个位置直接不能用。

过滤的是**违反硬约束的候选 Pose**。

------

## ① 越界过滤

例如：

衣柜：

宽180cm。

候选：

```
靠墙
但是右侧超出房间
```

删除。

------

## ② 门禁区域过滤

例如：

门：

```
入口
 |
 |
```

候选：

```
衣柜挡住门打开范围
```

删除。

------

## ③ 功能区域过滤

例如：

衣柜：

本体：

```
████
```

柜门开启区：

```
████████
```

如果候选导致：

柜门区域出房间：

删除。

------

## ④ 家具碰撞过滤

这个不是候选生成时知道的。

是在当前 Beam 状态里判断。

例如：

当前：

```
床
衣柜
```

生成书桌候选：

A：

```
窗边
```

通过。

B：

```
撞衣柜
```

删除。

------

## ⑤ 关系硬约束过滤

例如：

床头柜：

规则：

```
必须在床侧
```

那么：

随机放床头柜：

删除。

------

# 3. 影响步骤分 S(x)

这个不是删除。

而是：

> 都合法了以后，哪个更好。

例如：

两个衣柜候选：

------

A：

靠墙：

```
合法
```

评分：

+20

------

B：

也合法：

但是离墙10cm。

评分：

+5

------

所以：

过滤：

决定：

```
能不能存在
```

评分：

决定：

```
哪个更优
```

------

# 一个完整例子：书桌

配置：

```
{
type:"desk",

preferWindow:true,

chairRelation:"front",

avoidDoor:true
}
```

------

## 阶段1：生成候选

生成：

```
窗边位置1
窗边位置2
靠墙位置3
```

不会生成：

```
房间中央100个点
```

------

## 阶段2：过滤

候选1：

窗边，但是挡门：

❌ 删除

候选2：

窗边，没有问题：

✅ 保留

候选3：

靠墙，但是椅子区域放不下：

❌ 删除

------

## 阶段3：评分

剩下：

候选2：

```
靠窗 +30
椅子空间 +20
通行 +10

=60
```

进入 Beam。

------

# 最重要区别

| 阶段     | 配置作用      | 结果            |
| -------- | ------------- | --------------- |
| 候选生成 | 缩小搜索空间  | 少生成一些 Pose |
| 过滤     | 删除错误 Pose | 直接淘汰        |
| 评分     | 排序优先级    | 决定谁先进 Beam |

------

所以你之前说的：

> “不相关的东西可以不进入不相关区域内生成候选 Pose？”

对。

这是最高效的做法。

例如：

床尾凳：

不会先生成1000个位置，再发现桌子冲突。

而是：

先根据床尾关系：

生成：

```
床尾附近几个位置
```

然后再检查：

```
当前有没有桌子、椅子、通道冲突
```

两层结合：

**生成减少数量 + 过滤保证正确。**